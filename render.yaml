services:
  - type: web
    name: intern-performance-model
    runtime: python
    buildCommand: pip install -r requirements.txt && python run_pipeline.py
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
